services:
  dynamodb-local:
    image: amazon/dynamodb-local:latest
    container_name: servicenest-dynamodb-local
    ports:
      # Host 8001 avoids clash with other tools already on 8000
      - "8001:8000"
    command: ["-jar", "DynamoDBLocal.jar", "-sharedDb", "-inMemory"]
